{
  "buildCommand": "node scripts/generate-seo-assets.mjs && vite build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
